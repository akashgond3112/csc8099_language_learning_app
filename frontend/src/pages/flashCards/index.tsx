import { Box, Typography, useMediaQuery } from "@mui/material";
import React from "react";

const FlashCards = () => {
  return (
    <Box width={"100%"} height={"100%"} display={"grid"} gap={"1.5rem"}>
      <Typography>I am FlashCards</Typography>
    </Box>
  );
};

export default FlashCards;
